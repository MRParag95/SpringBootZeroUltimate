pluginManagement {
    repositories {
        maven { url = uri("https://repo.spring.io/snapshot") }
        gradlePluginPortal()
        mavenCentral()
        mavenLocal()
    }
}
rootProject.name = "SpringBootZeroUltimate"